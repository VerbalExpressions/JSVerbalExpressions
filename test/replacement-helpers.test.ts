import replace from "../src/replacement-helpers";
import VerEx from "../src/verex";
import "./custom-matchers";

describe("replace.afterMatch", () => {
  it.todo("should refer to the substring after the last match");
});

describe("replace.backReference(reference)", () => {
  it.todo("should refer to the capture indicated by the argument");
});

describe("replace.beforeMatch", () => {
  it.todo("should refer to the substring before the first match");
});

describe("replace.input", () => {
  it.todo("should refer to the string being operated on");
});

describe("replace.lastMatch", () => {
  it.todo("should refer to the last match");
});

describe("replace.lastParen", () => {
  it.todo("should refer to last capture");
});
